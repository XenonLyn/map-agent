{
  "name": "region-map-agent",
  "version": "0.3.0",
  "private": true,
  "description": "Regional map generation agent with a constraint verifier and self-repair loop (DSA4213 project demo)",
  "scripts": {
    "build": "python3 build.py",
    "test": "node tests/test_core.js && node tests/test_city.js",
    "screenshots": "python3 tests/screenshots.py 0"
  },
  "devDependencies": {
    "three": "0.128.0"
  }
}
